<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animationlesson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CustomAnimationTransitionActivity"></activity>
        <activity android:name=".SharedElementTransitionActivity" />
        <activity android:name=".SceneAnimationActivity" />
        <activity android:name=".FinanceProgressActivity" />
        <activity android:name=".ObjectAnimations" />
        <activity android:name=".ValueAnimationActivity" />
        <activity android:name=".AnimationsActivity" />
        <activity android:name=".ActivityAnimationDrawable" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>